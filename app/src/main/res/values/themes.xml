<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Ermonii" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Ermonii" parent="Base.Theme.Ermonii" />
    <style name="Theme.App" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="materialButtonStyle">@style/Button.App.Fast</item>
    </style>

    <style name="Button.App.Fast" parent="Widget.Material3.Button">
        <item name="android:layerType">hardware</item> <!-- Acelera renderizado -->
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="android:outlineSpotShadowColor" tools:targetApi="p">@android:color/transparent</item>
        <item name="android:outlineAmbientShadowColor" tools:targetApi="p">@android:color/transparent</item>
    </style>
</resources>